// n8n discovers the node and credential classes via the `n8n` field in
// package.json (pointing at the compiled files under dist/). This entry point
// only needs to exist for the `main` field.
module.exports = {};